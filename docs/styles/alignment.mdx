---
title: "Alignment"
description: "Documentation for Alignment"
---

The Phluts Alignment class represents a point within a rectangle. It's used to align child widgets within their parent.

## Available Values

| Value          | Description                                |
|----------------|--------------------------------------------|
| `topLeft`      | Top-left corner of the rectangle.          |
| `topCenter`    | Center of the top edge.                    |
| `topRight`     | Top-right corner of the rectangle.         |
| `centerLeft`   | Center of the left edge.                   |
| `center`       | Center of the rectangle.                   |
| `centerRight`  | Center of the right edge.                  |
| `bottomLeft`   | Bottom-left corner of the rectangle.       |
| `bottomCenter` | Center of the bottom edge.                 |
| `bottomRight`  | Bottom-right corner of the rectangle.      |

## Example

<CodeGroup>
```dart Dart
PhlutsAlign(
  alignment: PhlutsAlignment.center,
  child: PhlutsText(data: 'Centered'),
)
```

```json JSON
{
  "type": "align",
  "alignment": "center",
  "child": {
    "type": "text",
    "data": "Centered"
  }
}
```
</CodeGroup>

## Alignment Examples

### Top Alignments

<CodeGroup>
```dart Dart
// Top Left
PhlutsAlign(
  alignment: PhlutsAlignment.topLeft,
  child: PhlutsText(data: 'Top Left'),
)

// Top Center
PhlutsAlign(
  alignment: PhlutsAlignment.topCenter,
  child: PhlutsText(data: 'Top Center'),
)

// Top Right
PhlutsAlign(
  alignment: PhlutsAlignment.topRight,
  child: PhlutsText(data: 'Top Right'),
)
```

```json JSON
// Top Left
{
  "type": "align",
  "alignment": "topLeft",
  "child": { "type": "text", "data": "Top Left" }
}

// Top Center
{
  "type": "align",
  "alignment": "topCenter",
  "child": { "type": "text", "data": "Top Center" }
}

// Top Right
{
  "type": "align",
  "alignment": "topRight",
  "child": { "type": "text", "data": "Top Right" }
}
```
</CodeGroup>

### With Container

<CodeGroup>
```dart Dart
PhlutsContainer(
  width: 200.0,
  height: 200.0,
  color: PhlutsColors.grey,
  alignment: PhlutsAlignment.bottomRight,
  child: PhlutsText(data: 'Bottom Right'),
)
```

```json JSON
{
  "type": "container",
  "width": 200.0,
  "height": 200.0,
  "color": "#E0E0E0",
  "alignment": "bottomRight",
  "child": {
    "type": "text",
    "data": "Bottom Right"
  }
}
```
</CodeGroup>

### With Stack Positioned

<CodeGroup>
```dart Dart
PhlutsStack(
  children: [
    PhlutsContainer(color: PhlutsColors.blue),
    PhlutsAlign(
      alignment: PhlutsAlignment.center,
      child: PhlutsText(
        data: 'Overlay Text',
        style: PhlutsTextStyle(color: PhlutsColors.white),
      ),
    ),
  ],
)
```

```json JSON
{
  "type": "stack",
  "children": [
    { "type": "container", "color": "#2196F3" },
    {
      "type": "align",
      "alignment": "center",
      "child": {
        "type": "text",
        "data": "Overlay Text",
        "style": { "color": "#FFFFFF" }
      }
    }
  ]
}
```
</CodeGroup>

<Note>
For text-direction-aware alignment (RTL support), use [PhlutsAlignmentDirectional](/styles/phluts_alignment_directional) instead.
</Note>
