---
title: "Badge"
description: "Documentation for Badge"
---

import { PLAYGROUND_BASE_URL } from "/snippets/playground_base.mdx";

export const badgePreviewJson = {
  "type": "badge",
  "label": {
    "type": "text",
    "data": "5"
  },
  "child": {
    "type": "icon",
    "icon": "notifications",
    "size": 32
  },
  "backgroundColor": "#F44336",
  "textColor": "#FFFFFF"
};
export const badgePreviewSrc = `${PLAYGROUND_BASE_URL}/embed`;

The Phluts Badge allows you to build a Flutter Badge widget using JSON.
To know more about the Badge widget in Flutter, refer to the [official documentation](https://api.flutter.dev/flutter/material/Badge-class.html).

## Properties

| Property         | Type                      | Description                                                                                    |
|------------------|---------------------------|------------------------------------------------------------------------------------------------|
| backgroundColor  | `PhlutsColor`               | The badge's fill color (hex string). Defaults to `ColorScheme.error` from theme.               |
| textColor        | `PhlutsColor`               | The color of the badge's label text (hex string). Defaults to `ColorScheme.onError` from theme. |
| smallSize        | `double`                  | The diameter of the badge if [label] is null. Defaults to 6.0.                                |
| largeSize        | `double`                  | The badge's height if [label] is non-null. Defaults to 16.0.                                 |
| textStyle        | `PhlutsTextStyle`           | The text style for the badge's label.                                                         |
| padding          | `PhlutsEdgeInsets`          | The padding added to the badge's label. Defaults to 4 pixels horizontal.                       |
| alignment        | `PhlutsAlignmentGeometry`   | Combined with [offset] to determine the location of the [label]. Defaults to `AlignmentDirectional.topEnd`. |
| offset           | `PhlutsOffset`              | Combined with [alignment] to determine the location of the [label].                          |
| label            | `PhlutsWidget`              | The badge's content, typically a [PhlutsText] widget. If null, displays as a small filled circle. |
| count            | `int`                     | Convenience property for creating a badge with a numeric label. Automatically creates label showing count or '[maxCount]+' if count exceeds maxCount. |
| maxCount         | `int`                     | Maximum count value before showing '[maxCount]+' format. Only used when [count] is provided. Defaults to 999. |
| isLabelVisible   | `bool`                    | If false, the badge's [label] is not included. Defaults to `true`.                            |
| child            | `PhlutsWidget`              | The widget that the badge is stacked on top of. Typically an Icon or IconButton.              |

## Example

### Basic Badge with Label

<Tabs sync={false}>
  <Tab title="Dart">

```dart
PhlutsBadge(
  label: PhlutsText(data: '5'),
  child: PhlutsIcon(icon: 'notifications', size: 32),
  backgroundColor: '#F44336',
  textColor: PhlutsColors.white,
)
```

  </Tab>
  <Tab title="JSON">

```json
{
  "type": "badge",
  "label": {
    "type": "text",
    "data": "5"
  },
  "child": {
    "type": "icon",
    "icon": "notifications",
    "size": 32
  },
  "backgroundColor": "#F44336",
  "textColor": "#FFFFFF"
}
```

  </Tab>
  <Tab title="Preview">
    <Frame>
      <iframe
        id="phluts"
        src={badgePreviewSrc}
        title="Phluts Playground"
        className="w-full rounded-xl border-0"
        style={{ height: "640px" }}
        loading="lazy"
        onLoad={(event) => {
          const iframe = event.currentTarget;
          const targetOrigin = PLAYGROUND_BASE_URL;
          const message = {
            type: "phluts-preview-json",
            payload: badgePreviewJson
          };

          let attempts = 0;
          const maxAttempts = 12;
          const interval = setInterval(() => {
            iframe.contentWindow?.postMessage(message, targetOrigin);
            attempts += 1;

            if (attempts >= maxAttempts) {
              clearInterval(interval);
            }
          }, 250);
        }}
      />
    </Frame>
  </Tab>
</Tabs>
### Badge with Count

<CodeGroup>
```dart Dart
PhlutsBadge(
  count: 5,
  child: PhlutsIcon(icon: 'shopping_cart', size: 32),
)
```

```json JSON
{
  "type": "badge",
  "count": 5,
  "child": {
    "type": "icon",
    "icon": "shopping_cart",
    "size": 32
  }
}
```
</CodeGroup>

### Badge with Count Exceeding MaxCount

<CodeGroup>
```dart Dart
PhlutsBadge(
  count: 1000,
  maxCount: 99,
  child: PhlutsIcon(icon: 'notifications', size: 32),
)
```

```json JSON
{
  "type": "badge",
  "count": 1000,
  "maxCount": 99,
  "child": {
    "type": "icon",
    "icon": "notifications",
    "size": 32
  }
}
```
</CodeGroup>

### Small Badge (No Label)

<CodeGroup>
```dart Dart
PhlutsBadge(
  smallSize: 8,
  backgroundColor: '#F44336',
  child: PhlutsIcon(icon: 'circle', size: 32),
)
```

```json JSON
{
  "type": "badge",
  "smallSize": 8,
  "backgroundColor": "#F44336",
  "child": {
    "type": "icon",
    "icon": "circle",
    "size": 32
  }
}
```
</CodeGroup>

### Badge on IconButton

<CodeGroup>
```dart Dart
PhlutsBadge(
  count: 3,
  child: PhlutsIconButton(
    icon: PhlutsIcon(icon: 'notifications', size: 24),
    padding: PhlutsEdgeInsets.all(0),
    onPressed: PhlutsNoneAction(),
  ),
)
```

```json JSON
{
  "type": "badge",
  "count": 3,
  "child": {
    "type": "iconButton",
    "icon": {
      "type": "icon",
      "icon": "notifications",
      "size": 24
    },
    "padding": {
      "left": 0,
      "top": 0,
      "right": 0,
      "bottom": 0
    },
    "onPressed": {
      "actionType": "none"
    }
  }
}
```
</CodeGroup>

### Badge with Custom Styling

<CodeGroup>
```dart Dart
PhlutsBadge(
  label: PhlutsText(data: 'NEW'),
  backgroundColor: PhlutsColors.green,
  textColor: PhlutsColors.white,
  largeSize: 20,
  padding: PhlutsEdgeInsets(left: 8, top: 4, right: 8, bottom: 4),
  alignment: PhlutsAlignment(dx: 1.0, dy: -1.0),
  offset: PhlutsOffset(dx: 4, dy: -4),
  child: PhlutsIcon(icon: 'mail', size: 32),
)
```

```json JSON
{
  "type": "badge",
  "label": {
    "type": "text",
    "data": "NEW"
  },
  "backgroundColor": "#4CAF50",
  "textColor": "#FFFFFF",
  "largeSize": 20,
  "padding": {
    "left": 8,
    "top": 4,
    "right": 8,
    "bottom": 4
  },
  "alignment": {
    "dx": 1.0,
    "dy": -1.0
  },
  "offset": {
    "dx": 4,
    "dy": -4
  },
  "child": {
    "type": "icon",
    "icon": "mail",
    "size": 32
  }
}
```
</CodeGroup>
