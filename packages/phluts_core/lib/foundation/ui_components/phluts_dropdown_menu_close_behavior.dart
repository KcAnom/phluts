/// Controls how a [DropdownMenu] responds to taps outside of it.
enum PhlutsDropdownMenuCloseBehavior {
  /// Close when tapping anywhere.
  all,

  /// Close only when tapping the menu itself.
  self,

  /// Do not close automatically.
  none,
}
