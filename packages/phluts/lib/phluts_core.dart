/// Pure-Dart entry point for Phluts screens.
///
/// Use this import in files annotated with `@PhlutsScreen` to ensure they
/// can be processed by the Phluts CLI without requiring the Flutter SDK.
library;

export 'package:phluts_core/phluts_core.dart';
